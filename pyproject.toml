[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "N", "UP", "B", "C4", "SIM"]

[tool.ruff.lint.per-file-ignores]
# FastAPI 惯例：Body(...)/File(...) 必须写在参数默认值里
"app.py" = ["B008"]

[tool.black]
line-length = 100
target-version = ["py311"]
